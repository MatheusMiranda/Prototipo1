
<%= form_for @public_agency do |form| %>
<% if @public_agency.errors.any? %>
	<div id="error_explanation">
		<h2> <%= pluralize(@public_agency.errors.count, "erro") %>não permintiu salvar</h2>
		<ul>
		<% @public_agency.errors.full_messages.each do |mensage|%>
		<li><%= mensage %></li>
		<%end%>
		</ul>
		</div>
		<% end %>
<center>
<table>
<tr>
		<p>
		
		<td><%= form.label :name %></td>
		<td><%= form.text_field :name %></td>
		</p>
		</tr>
		<tr>
		<p>
		<td><%= form.label :description %></td>
		<td><%= form.text_area :description %></td>
		</p>
		</tr>
</table>
</center>
		<%= form.submit %>

		<% end %>
